import { LogOut, PlusCircle } from 'lucide-react';
import { useAuth } from '../hooks/useAuth';
export default function Header({ onCreate }:{ onCreate:()=>void }){ const { email, role, logout } = useAuth(); return (<header className='sticky top-0 z-10 bg-white/80 backdrop-blur border-b border-slate-200'><div className='max-w-6xl mx-auto px-4 py-3 flex items-center justify-between'><div className='text-xl font-semibold tracking-tight'>Task Manager</div><div className='flex items-center gap-3 text-sm'><span className='hidden sm:block text-slate-600'>{email} • <span className='font-medium'>{role}</span></span><button onClick={onCreate} className='inline-flex items-center gap-2 rounded-lg border border-slate-300 px-3 py-1.5 hover:bg-slate-50'><PlusCircle className='w-4 h-4'/> New Task</button><button onClick={logout} className='inline-flex items-center gap-2 rounded-lg bg-slate-900 text-white px-3 py-1.5 hover:bg-slate-800'><LogOut className='w-4 h-4'/> Logout</button></div></div></header>); }